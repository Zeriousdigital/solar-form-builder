{
  "name": "solar-form-builder-backend",
  "version": "1.0.0",
  "scripts": {
    "dev": "ts-node-dev src/index.ts",
    "build": "npx prisma generate --schema=src/prisma/schema.prisma && tsc",
    "start": "node dist/index.js",
    "postinstall": "npx prisma generate --schema=src/prisma/schema.prisma"
  },
  "dependencies": {
    "express": "^4.18.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "@prisma/client": "^5.0.0",
    "axios": "^1.6.0",
    "helmet": "^7.0.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.0",
    "@types/cors": "^2.8.0",
    "@types/node": "^20.0.0",
    "prisma": "^5.0.0",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.0.0"
  }
}
