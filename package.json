{
  "name": "solar-form-builder",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cd ../backend && npm install && npx prisma generate --schema=src/prisma/schema.prisma && npm run build",
    "start": "cd backend && node dist/index.js",
    "dev": "cd backend && npm run dev"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
